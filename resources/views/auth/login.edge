@layout('layouts/app')
@set('title', 'Login page')
@section('login')
@set('borderColor', 'input-primary')
@if(flashMessages.has('error'))
@set('borderColor', 'input-error')
@else
@set('borderColor', 'input-primary')
@end 
  <section class="flex justify-center w-full h-screen items-center">
    <div class="form-control w-full max-w-xs">    
        <form action="{{ route('auth.login') }}" method="POST">
        <label class="label">
          <span class="label-text">Username or Email</span>
        </label>
         <input type="text" name="uid" placeholder="Username or Email" class="input input-bordered {{borderColor}} w-full max-w-xs">
        <label class="label">
            <span class="label-text">Password</span>
        </label>
        <input type="password" name="password" placeholder="Password" class="input input-bordered {{borderColor}} w-full max-w-xs">
        <p class="text-error-content my-3 text-sm">{{ flashMessages.get('error', '') }}</p>
        <button class="btn btn-primary btn-block btn-square" type="submit">Login</button>
        </form>
    </div>
</section>
@end